clear

whd = imdb_choosedb_unwrap;

load(fullfile(whd,'im_params.mat'));
load('gantry_centrad.mat','unwrapparams')

ims = zeros([size(unwrapparams.mX),
for yi = 1:length(p.ys)
    for xi = 1:length(p.xs)
    end
end